# medfit API Design Decisions → moved

> **Moved to the ecosystem hub (2026-06-10).** The API design decisions recorded
> here (S7 object system, hybrid S3 generics, `mediate()`/`boot()`/`paths()` naming,
> standard R generics, broom integration, selective loading, CMAverse adapter)
> became the ecosystem-wide API conventions that all mediation packages follow, so
> the record now lives in the hub:
>
> `mediation-planning/docs/design/API-DESIGN-DECISIONS.md`
> (index: `mediation-planning/docs/design/README.md`)
>
> medfit implements these decisions; medfit-specific roadmap detail is in
> `planning/medfit-roadmap.md`.
