# Register lavaan Method for extract_mediation

This function is called from `.onLoad()` to register the S7 method for
lavaan objects when the lavaan package is available.

## Usage

``` r
.register_lavaan_method()
```
