% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics-effects.R
\name{joint_effects}
\alias{joint_effects}
\title{Joint Effects at a Given Parameter Vector}
\usage{
joint_effects(object, estimates = object@estimates)
}
\arguments{
\item{object}{A \link{JointMediationData} object.}

\item{estimates}{Named numeric vector with the prefixed source rows of
\code{object@estimates} (alias rows are ignored).}
}
\value{
Named numeric vector: \code{cde}, \code{nde}, \code{nie}, \code{te}.
}
\description{
Recomputes the joint CDE, NDE, NIE and total effect of a
\link{JointMediationData} object from a named parameter vector, holding the
object's structure, reference levels (\code{m_star}) and sample covariate means
fixed. With the default \code{estimates = object@estimates} it returns the stored
effects. Its main use is as the statistic of a parametric bootstrap: the
draws \code{\link[=bootstrap_mediation]{bootstrap_mediation()}} passes to \code{statistic_fn} are named like
\verb{@estimates}, and the NDE needs the prefixed intercept and covariate rows
(\code{m1_}, ..., \code{y_}) and the covariate means, not only the path aliases.
}
\examples{
d <- mediation_demo
fit <- extract_mediation(
  lm(mediator1 ~ treatment + covariate1 + covariate2, d),
  model_y = lm(outcome_int ~ treatment * mediator1 + mediator2 +
                 covariate1 + covariate2, d),
  treatment = "treatment", mediator = c("mediator1", "mediator2"),
  mediator_models = list(lm(mediator2 ~ treatment + mediator1 +
                              covariate1 + covariate2, d))
)
joint_effects(fit)

# Parametric bootstrap of the joint NIE
boot <- bootstrap_mediation(
  function(theta) joint_effects(fit, theta)[["nie"]],
  method = "parametric", mediation_data = fit, n_boot = 500, seed = 1
)
boot@ci_lower
boot@ci_upper
}
