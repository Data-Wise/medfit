% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics-effects.R
\name{nie}
\alias{nie}
\title{Extract Natural Indirect Effect (NIE)}
\usage{
nie(x, ...)
}
\arguments{
\item{x}{A \link{MediationData}, \link{SerialMediationData}, \link{ParallelMediationData},
\link{InteractionMediationData}, \link{JointMediationData}, or \link{BootstrapResult}
object. For a \code{BootstrapResult}, \code{nie()} returns the bootstrapped point
estimate (with a warning if the statistic was not an NIE).}

\item{...}{Additional arguments passed to methods. For a
\code{SerialMediationData}, \code{type = c("chain", "total")} selects the
chain-specific (default) or the total indirect effect (see Details).}
}
\value{
A numeric scalar of class \code{mediation_effect} carrying a \code{type}
attribute. For intervals use \code{\link[=confint]{confint()}} or \code{\link[=bootstrap_mediation]{bootstrap_mediation()}}.
}
\description{
Extract the natural indirect effect from a mediation analysis result.
The NIE represents the effect of treatment on outcome that operates
through the mediator(s).
}
\details{
The effects are for a unit contrast of the treatment. By class:
\itemize{
\item \code{MediationData}: \eqn{NIE = a b}{NIE = a * b}.
\item \code{SerialMediationData}: with \code{type = "chain"} (the default), the
effect through the full chain only,
\eqn{NIE = a \, d_1 \cdots d_{k-1} \, b}{NIE = a * d1 * ... * d(k-1) * b};
with \code{type = "total"}, the total indirect effect, the sum over every
treatment-to-outcome path through at least one mediator (including paths
that skip a mediator, such as X -> M2 -> Y), which equals
\code{te(x) - nde(x)}.
\item \code{ParallelMediationData}: \eqn{NIE = \sum_j a_j b_j}{NIE = sum(a_j * b_j)}.
\item \code{InteractionMediationData}: \eqn{NIE = INTmed + PIE =
    (\theta_2 + \theta_3) \beta_1}{NIE = INTmed + PIE = (t2 + t3) * b1}.
\item \code{JointMediationData}: the joint NIE through all the mediators,
\eqn{\sum_i (\theta_{2i} + \theta_{3i}) \beta^*_{1i}}{sum((t2i + t3i) * b1i*)}.
}
The "Methods and Formulas" article on the package website gives the full
formulas.
}
\examples{
med_data <- fit_mediation(
  formula_y = outcome ~ treatment + mediator1 + covariate1 + covariate2,
  formula_m = mediator1 ~ treatment + covariate1 + covariate2,
  data = mediation_demo,
  treatment = "treatment",
  mediator = "mediator1"
)

nie(med_data)

}
\seealso{
\code{\link[=nde]{nde()}}, \code{\link[=te]{te()}}, \code{\link[=pm]{pm()}}, \code{\link[=paths]{paths()}}, \code{\link[=decompose]{decompose()}}
}
