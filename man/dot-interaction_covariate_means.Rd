% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-lm.R
\name{.interaction_covariate_means}
\alias{.interaction_covariate_means}
\title{Covariate means for the reference mediator mean (four-way decomposition)}
\usage{
.interaction_covariate_means(dat, covs, mm = NULL)
}
\arguments{
\item{dat}{Data frame (the object's \verb{@data}), or \code{NULL}.}

\item{covs}{Covariate coefficient names (mediator model, excluding the
intercept and the treatment).}

\item{mm}{Optional design matrix of the mediator model.}
}
\value{
Named numeric vector over \code{covs}.
}
\description{
Sample means of the mediator model's covariate design columns: from \code{mm}
when given (the extractor passes \code{model.matrix(model_m)}), else rebuilt
from \code{dat} -- a model frame via its \code{terms} attribute, or caller data via
the \code{medfit_design} attribute the extractor attaches -- else plain numeric
columns of \code{dat} (e.g. the lavaan data matrix). Uses \code{\link[=mean]{mean()}} per column so
numeric-covariate results match the column means exactly.
}
\keyword{internal}
