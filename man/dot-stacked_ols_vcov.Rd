% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-joint.R
\name{.stacked_ols_vcov}
\alias{.stacked_ols_vcov}
\title{Stacked-OLS covariance of several equations fit to the same rows}
\usage{
.stacked_ols_vcov(models, prefixes)
}
\arguments{
\item{models}{List of fitted lm/glm (Gaussian identity) models.}

\item{prefixes}{Character prefixes for the stacked coefficient names.}
}
\value{
Named covariance matrix of the stacked coefficients.
}
\description{
Diagonal blocks are each model's \code{\link[stats:vcov]{stats::vcov()}}. Off-diagonal blocks are
\eqn{\hat\sigma_{ef} (X_e'X_e)^{-1} X_e'X_f (X_f'X_f)^{-1}}{s_ef (Xe'Xe)^-1 Xe'Xf (Xf'Xf)^-1},
with the residual cross-product divided by
\eqn{\sqrt{(n - p_e)(n - p_f)}}{sqrt((n - pe)(n - pf))}, so the formula
reduces to \code{vcov()} on the diagonal. A block is exactly zero when one
equation's residual lies in the other's column space (serial chains with
shared covariates; the outcome against every mediator).
}
\keyword{internal}
