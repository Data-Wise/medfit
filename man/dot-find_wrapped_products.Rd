% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-lm.R
\name{.find_wrapped_products}
\alias{.find_wrapped_products}
\title{Find function-wrapped product terms}
\usage{
.find_wrapped_products(models, vars, require_all = FALSE)
}
\arguments{
\item{models}{List of fitted lm/glm models (\code{NULL} entries are skipped).}

\item{vars}{Character vector: treatment and mediator names.}

\item{require_all}{Logical: flag only terms involving every name in \code{vars}.}
}
\value{
\code{"<response>: <term>"} labels, or \code{character(0)}.
}
\description{
A product written inside a function call, such as \code{I(X * M)}, is a single
order-1 term, so the \code{":"}-based scans miss it and the product is treated as
an unrelated covariate. This flags order-1 terms whose variables (via
\code{\link[=all.vars]{all.vars()}}) include at least two distinct names and involve \code{vars}: any of
them by default, or all of them when \code{require_all = TRUE}. Single-variable
transforms such as \code{I(X^2)} or \code{log(C)} are not flagged. Products precomputed
as a data column cannot be detected from the formula.
}
\keyword{internal}
